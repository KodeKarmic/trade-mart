<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <!-- Minimal Checkstyle configuration (safe): keep TreeWalker present so custom/extended configs
         can be added separately without breaking the main build. -->
    <module name="TreeWalker"/>
</module>
